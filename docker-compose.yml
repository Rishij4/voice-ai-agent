version: '3'

services:
  postgres:
    image: postgres
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_DB: healthcare_ai
    ports:
      - "5432:5432"

  redis:
    image: redis
    ports:
      - "6379:6379"